# 安全政策（Security Policy）

## 支持范围

本项目处于 `0.x` 实验阶段，只维护 `main` 分支上的最新代码。

## 报告漏洞

请不要开公开 issue。用 GitHub 的私密报告渠道：

> 仓库页面 → **Security** 标签 → **Report a vulnerability**

或者在 issue 里只写"有一个安全问题，请提供私密联系方式"，不要贴细节。

请尽量包含：受影响的版本/提交、复现步骤、影响面（提权 / 崩溃 / 数据泄漏）、
以及你建议的修复方向。我们会在确认后于 CHANGELOG 与 release note 里致谢
（除非你希望匿名）。

## 已知的"非安全问题"

下面这些是**已记录的已知问题**，不是安全漏洞，请走普通 issue：

| 现象 | 说明 |
|---|---|
| `dmesg` 出现 `taints kernel`、`/proc/sys/kernel/tainted` 非 0 | 外部模块加载的正常结果（`O` 标记），见 README |
| 加载/卸载驱动后整机"能 ping 不能 SSH" | 已知的稳定性问题与规避方法见 README「已知问题」 |
| 芯片挂死（`-110`）只能物理拔插 | 已知的硬件/固件行为 |
| Secure Boot 下未签名模块被拒 | 预期行为，需注册 MOK，见 README |

## 供应链注意事项

- 仓库**不包含**厂商固件、Windows 驱动或任何二进制 blob；固件请从你自己设备的
  配套材料取得，并用 `scripts/install-firmware.sh` 校验 SHA-256
- 模块签名私钥（`MOK.priv` 等）只应存在于本机，已被 `.gitignore` 排除；
  泄露私钥等于允许他人给内核签名，**发现泄露请立即吊销并重新生成**
- 如果你在仓库里发现了被误提交的密钥/凭据，请按上面的渠道私密报告
